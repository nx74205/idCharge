<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:title="Save"
        android:id="@+id/save_page"
        android:orderInCategory="100"
        app:showAsAction="ifRoom"
        android:icon="@drawable/commit_change"/>

    <item android:title="Delete"
        android:id="@+id/delete_entry"
        android:orderInCategory="110"
        app:showAsAction="ifRoom"
        android:icon="@drawable/delete_entry"/>

    <item android:title="Refresh"
          android:id="@+id/refresh_data"
          android:orderInCategory="120"
          app:showAsAction="always"
          android:icon="@drawable/refresh_data"/>
</menu>